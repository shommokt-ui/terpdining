// Edit this list to change what shows in the announcement banner.
// Each entry needs a unique `id` — once a user dismisses one, that id is
// remembered in localStorage so it won't show again on this device.
// Delete an entry (or bump the `id`) to force it to reappear for everyone.

const announcements = [
  // {
  //   id: 'summer-2026-hours',
  //   type: 'info', // 'info' | 'warning'
  //   message: '251 North is closed for the summer session. South Campus and Yahentamitsi are open as usual.',
  // },
];

export default announcements;
